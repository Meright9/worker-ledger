{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：核心能力 + 自定义账本读写命令",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "allow-ledger-load",
    "allow-ledger-save",
    "allow-ledger-export"
  ]
}
